import React from "react";
import {PokerGameState} from "../../models/PokerGameState";

interface GameListProps {
    game: PokerGameState[];
}

export const PokerTable: React.FunctionComponent<GameListProps> = ({game}) => {

    return (
        <div></div>
    )
}